# Dependencies
node_modules/
vendor/
.venv/
venv/
backend/venv/
__pycache__/

# TS build artifacts
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Build output
dist/
build/
out/
.next/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Coverage
coverage/
.nyc_output/

# Runtime
*.pid
*.seed
.claude/worktrees/

# SQLite test/dev databases
*.db
*.db-journal

# Real-footage fixtures — large binaries, regenerate via yt-dlp.
# See docs/phase-completions/0-real-footage-validation.md "Reproduction" for the exact command.
agents/capture/fixtures/real/*.mp4
agents/capture/fixtures/real/*.webm
agents/capture/fixtures/real/*.mkv
# Coverage clip corpus + derived frame dataset — large binaries (All-22 clips
# + extracted .jpg frames). Corpus lives in the Projects clone; dataset is
# regenerable via agents/capture/extract_coverage_frames.py.
agents/capture/fixtures/coverage/*.mp4
agents/capture/coverage_dataset/
# Validation report JSONs — kept small and committed; only the giant
# media files are excluded above.

# Per-service Python venvs. Each service has its own; do not commit.
services/*/.venv/
services/*/venv/

# Sub-task 1 yt-dlp invocation rolling log — local diagnostic only.
scripts/hud_calibration/training_clips_invocations.log
