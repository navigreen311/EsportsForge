"""AnimaForge integration service package.

# STUB FOR BRANCH-LOCAL IMPORTS — Agent #1 owns the canonical __init__.py.

This empty package marker only exists so Agent #2's
``app.services.animaforge.notifications`` module is importable on this
branch in isolation. Agent #1's branch contains the real re-exports for
``client``, ``exceptions``, etc. — at merge, Agent #1's version replaces
this file.
"""
