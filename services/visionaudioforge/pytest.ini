[pytest]
# `pythonpath = .` puts services/visionaudioforge on sys.path so the suite's
# `from app...` imports resolve when running a bare `pytest` from this dir — no
# PYTHONPATH env needed (locally or in CI). Requires pytest >= 7 (VAF pins 8.3.3).
testpaths = tests
pythonpath = .
